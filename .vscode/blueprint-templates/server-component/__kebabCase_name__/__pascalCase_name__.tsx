import { BasePageProps } from "@/utils/AppType";

import React from "react";

import styles from "./{{pascalCase name}}.module.scss";

export default function {{pascalCase name}}Page({params, searchParams}: BasePageProps) {
    return (
        <div></div>
    )
}